<div class="cta-card cta-{{type}}">
    <a href="{{url}}" class="cta-link" {{#if trackingId}}data-tracking="{{trackingId}}"{{/if}} target="_blank" rel="noopener noreferrer">
        <span class="cta-text">{{text}}</span>
        <span class="cta-arrow">→</span>
    </a>
</div>
